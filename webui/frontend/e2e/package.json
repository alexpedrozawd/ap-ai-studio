{
  "name": "webui-e2e-manual",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Verificacao visual/interativa manual da webui (Chromium headless real via Playwright, nao mockado). Fora do CI/pre-commit de proposito - roda so' quando alguem (ou o Claude) decide conferir visualmente ou interagir de verdade com a interface.",
  "scripts": {
    "check": "node visual-check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
